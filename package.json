{
  "name": "musical-choir-website",
  "version": "4.0.0",
  "description": "Сайт Broadway Guitardo — Vue + Express",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "build": "npm install --include=dev --prefix client && npm run build --prefix client",
    "render-build": "npm install && npm install --include=dev --prefix client && npm run build --prefix client",
    "dev:api": "node backend/server.js",
    "dev:client": "npm run dev --prefix client",
    "dev": "echo Запустите: npm run dev:api и npm run dev:client в двух терминалах",
    "init-auth": "node scripts/init-auth.js",
    "pages:build": "node scripts/build-pages.mjs",
    "pages:dev": "npm run pages:build && wrangler pages dev dist",
    "pages:deploy": "npm run pages:build && wrangler pages deploy dist"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@libsql/client": "^0.17.4",
    "aws4fetch": "^1.0.20",
    "bcryptjs": "^3.0.2",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "hono": "^4.6.14",
    "multer": "^1.4.5-lts.1",
    "sharp": "^0.34.3"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
